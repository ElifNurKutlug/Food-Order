const express = require("express");
const burgerModel = require("./");
const router = express.Router();

//Get All Foods Servisi
app.get("/getFoods", async (req, res) => {
  try {
    const foods = await burgerModel.find({});
    res.send(foods);
  } catch (err) {
    console.log(err);
  }
});
